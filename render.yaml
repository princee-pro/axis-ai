services:
  - type: web
    name: axis-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m jarvis_ai.mobile.server
    envVars:
      - key: SERVER_HOST
        value: 0.0.0.0
      - key: PYTHON_VERSION
        value: 3.11.0
